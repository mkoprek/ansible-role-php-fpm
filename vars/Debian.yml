---
apt_repository: deb https://packages.sury.org/php/ {{ ansible_distribution_release|lower }} main
apt_repository_key: https://packages.sury.org/php/apt.gpg

required_packages:
  - apt-transport-https
  - gnupg2
